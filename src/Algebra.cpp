#include "Algebra.hpp"


Index HashJoinAndAggregate::eval()
{
    /* TODO
     * For every record in RHS, probe the index on LHS.
     * For every match, extract the aggregation key and aggregate into the new index I.
     * Return I.
     */
}
